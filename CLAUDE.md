# KirinDesk Working Principles

## 1. Phase-Based Execution

KirinDesk must be developed phase by phase.

Do not make large, cross-cutting changes without approval.

Before any code change, always provide:

- implementation scope
- files to be added or modified
- database changes
- risks
- rollback plan
- validation commands
- acceptance criteria

Do not proceed until the user confirms.

## 2. Karpathy-Style Working Principles

Follow a simple, incremental, evidence-driven engineering style.

Core rules:

- Make the simplest thing work first.
- Prefer boring, explicit, debuggable solutions over clever abstractions.
- Avoid premature architecture unless the next phase clearly needs it.
- Build in small steps.
- Verify each step with commands, tests, logs, or direct inspection.
- Keep the system understandable to a small team.
- Do not hide complexity behind vague "magic".
- Do not claim something works unless it has been verified.
- Prefer readable code and clear data flow over over-engineered patterns.
- When uncertain, inspect the current files and state before editing.
- When a test fails, explain the cause before changing more code.
- Never fix unrelated problems silently.
- Never widen the scope without explicit approval.

## 3. Safety and Trust First

KirinDesk handles sensitive business data, including customers, orders, suppliers, pricing, profit, commission, files, and internal workflows.

The product must be designed around customer trust.

Rules:

- Customer data belongs to the customer.
- KirinDesk must not use customer business data for its own sales, resale, customer poaching, or public AI training.
- Platform admins must not have unrestricted default access to tenant business data.
- Future support access must require customer authorization, reason, scope, time limit, and audit logs.
- Sensitive actions must be server-side controlled, not only hidden in the UI.
- File access, downloads, exports, permission changes, and platform access must be auditable.
- Do not use exaggerated claims such as "absolute security", "bank-level security", "military-grade security", or "ISO/SOC certified" unless actually certified.

Preferred external wording:

- controllable
- auditable
- exportable
- removable
- private-deployment-ready
- built with mature security principles over time

## 4. SaaS Architecture Rules

KirinDesk must be designed as a multi-tenant SaaS from day one.

Rules:

- LETPCBA is only the first tenant. Never hardcode LETPCBA into system logic.
- All tenant business tables must include tenant_id.
- Tenant isolation must be enforced at both application and database design levels.
- PostgreSQL RLS must be planned before implementing tenant business tables.
- RBAC, data scope, and field-level response filtering are required.
- Role-based UI hiding is not enough; backend DTO/API responses must not leak fields.
- Platform admin identity must be separated from tenant user identity.

## 5. Database and Migration Rules

Before creating or changing database schema:

- provide table list
- explain tenant_id usage
- explain RLS strategy
- explain indexes
- explain soft delete strategy
- explain audit requirements
- explain rollback strategy

Do not create migrations without explicit confirmation.

Do not use production credentials.

Do not run destructive database commands unless explicitly approved.

## 6. Audit and Compliance Rules

KirinDesk must support append-only audit logs with hash-chain design in later phases.

Sensitive operations must be designed to be auditable, including:

- login
- permission changes
- user changes
- tenant changes
- file upload
- file download
- export
- support access
- approval
- lock/unlock
- business data changes
- AI/OCR calls

Audit logs must not be editable or deletable by normal application logic.

## 7. Provider Abstraction Rules

AI, OCR, Payment, Storage, and Commission logic must use provider interfaces.

Do not hardcode vendors.

Phase 0 may only use mock providers or interface placeholders.

Do not connect real DeepSeek, OpenAI, WeChat Pay, Alipay, Stripe, OCR services, cloud storage, or email services without approval.

## 8. Local Development Rules

Development starts locally.

Do not deploy to a public server unless explicitly approved.

Do not kill unrelated processes.

Do not run killall node.

If a port is occupied, report the conflict and suggest options.

Do not delete .claude/.

Do not commit .env, node_modules, dist, logs, private storage files, or real secrets.

## 9. Reporting Rules

After each phase or sub-phase, report:

- files added
- files modified
- files deleted
- commands executed
- test results
- what passed
- what failed
- whether database schema changed
- whether any secrets were created
- whether any risk remains
- recommended next step

If something failed, report the exact failure and propose the smallest safe fix.

## 10. Current Phase Rule

The current completed execution scope is:

- Phase 0: Foundation baseline established — monorepo skeleton, Docker Compose local env (PostgreSQL + Redis), trust & security docs, and the rules/docs for database, tenant isolation, auth, RBAC, RLS, audit, file security, and provider abstraction. Database migrations, dual-JWT auth, RBAC, audit hash-chain, and the quality gate / test infrastructure have landed in code.
- Phase 1A: Customers — table (RLS + indexes), API (CRUD + soft delete + dataScope + audit), and integration tests. Web CRUD pages added later (commit 2a6b4ad). Committed.
- Phase 1B: Sales Orders — table, API, and web CRUD pages. Frontend CRUD browser QA (T01–T13) passed: create / list / filter / search / edit / soft-delete, with 400 / 401 / 404 / 409 verified. Committed (latest commit 54aa2be).
- Phase 1C: Suppliers — table (RLS + indexes), suppliers:* permissions (procurement module), API (CRUD + soft delete + dataScope + audit), web CRUD pages, and integration tests. Integration suite 101 passing; browser QA (create / list / filter / search / edit / soft-delete / nav) and server-side 400 / 401 / 404 verified. Committed (commit 76363c6).
- Phase 1D: Purchase Orders — table (RLS + indexes, supplier_id FK ON DELETE RESTRICT, unique tenant order_number), procurement:delete permission, API (CRUD + soft delete + dataScope + supplier in-scope check + duplicate order_number 409 + audit), web CRUD pages with supplier dropdown, and integration tests. Full quality gate green (lint / format / typecheck / build / unit 7 / integration 129 / security 13); browser QA (create / supplier dropdown / name mapping / search / edit / soft-delete / nav) and server-side 400 / 401 / 404 / 409 verified. Committed (commit 0d9b082).
- Phase 1E: Files (object storage) — S3-compatible storage behind a provider interface (StorageProvider + S3StorageProvider, MinIO for local dev via docker-compose), files API (multipart upload with 25MB + MIME allowlist + server-side sha256, list + getOne + dataScope, single-use short-lived signed download tokens hashed at rest, public token-authenticated download endpoint, soft delete), files:view permission seed, migration 028 (user FKs + app_lookup_file_token SECURITY DEFINER helper for anonymous-download RLS bootstrap), migration 029 (pin SECURITY DEFINER search_path), and integration tests with an in-memory fake storage provider. Full quality gate green (lint / format / typecheck / build / unit 7 / integration 152 / security 13); end-to-end QA against real MinIO (upload → object lands with tenant-prefixed key → list → token download byte-match → token reuse 404 → MIME reject 400 → soft delete → audit chain uploaded/token_issued/downloaded/deleted) verified. Self-review (security/isolation, error-handling/concurrency, audit/provider/secrets) completed; two must-fix items (SECURITY DEFINER search_path, S3 SDK error scrubbing) resolved.

  Known tech debt deferred from Phase 1E (revisit in later phases, not blocking):
  - Tenant status gate on download: a token issued before a tenant is suspended remains usable for its short TTL. Belongs in a global tenant-lifecycle middleware, not the files module.
  - Download token rate limiting: no cap on how many active tokens can be minted per file. Belongs in global rate-limiting middleware.
  - Real-time permission revocation on download: download validates token validity only, not whether the issuer still holds files:download / the file is still in scope at download time. Inherent signed-token trade-off (short TTL + single-use mitigates).
  - Orphan object sweep: a failed metadata insert after a successful storage put attempts a best-effort delete; double-failure leaves an orphan object (logged, no DB row references it). Needs a background reconciliation job (storage vs files table).
  - sha256 de-duplication: identical content uploaded multiple times produces multiple rows/objects; sha256 is computed and stored but not used for dedup. Product decision (cross-user object sharing touches isolation).
  - Audit metadata enrichment: file.downloaded / file.token_issued record actorId + resourceId only; downloader IP/UA and token id are not captured. Audit enhancement pending schema confirmation.

- Phase 1F-A: Order line items — migration 030 (sales_order_items + purchase_order_items detail tables with RLS + indexes + quantity/price/line_total numeric precision checks), backend for both sales and purchase orders (line items written in a single transaction, total_amount derived server-side as Σ line_total and never accepted from the client, line_no assigned server-side, draft allows 0 lines / non-draft requires ≥1, full-array replace semantics on update, audit before/after snapshots include full line-item sets, historical header-only orders still readable/editable), shared BigInt money helper (apps/api/src/common/order-money.ts) reused across both services, and a web line-item editor on both order forms (add/remove rows, per-row line_total + read-only derived total_amount, items[] sent on submit, existing lines echoed on edit). Full quality gate green (lint / format / typecheck / build / unit 15 / integration 154 / security 13); pushed (commits fb31e93 backend, 51e7d08 web). Browser QA still to be done.

- Phase 1F-B: Finance / exchange-rate snapshot — orders capture a base-currency cost view via an FX rate snapshot at order time so downstream reporting and commission can sum in the tenant base currency. Each order derives total_amount_base from the captured rate (NULL when un-costed, never silently zeroed); the BigInt money helper (apps/api/src/common/order-money.ts) is shared across sales and purchase order services. Pushed (commit 2559076 backend).

- Phase 1F-C: Order approval workflow — migration 032 (order_approvals shared immutable ledger with an order_type discriminator, no hard FK, RLS + tenant_isolation, SELECT/INSERT-only grant, level column reserved for multi-level; both order status CHECKs widened to the new superset, down migration coerces straggler states before restoring). Shared state machine (common/order-approval.ts) with submit/approve/reject/withdraw transitions: assertTransition → 409 on illegal, ApprovalScope guard (403, all-scope required), SelfApproval guard (403, approver ≠ submitter); reject requires a reason. Each transition runs in one tenant-context tx (SELECT … FOR UPDATE, validate, UPDATE status, INSERT ledger row, audit after commit), wired into both sales and purchase order controllers. Pushed (commit f564687 backend).

- Phase 1F-D: Reports aggregation — read-only sales/purchase summary reports over existing orders + FX base currency, on-demand SQL aggregation (no migration, no materialized view). GET /api/reports/sales-summary and /purchase-summary gated by reports:view; SUM(total_amount_base) in tenant base currency with NULL-base (un-costed) rows excluded from the sum and surfaced as a separate count; status calibers (realized / approved_up / pipeline / all, cancelled never summed); groupBy status/customer/supplier/period at month/day granularity; dataScope pushed into the aggregation WHERE before GROUP BY (own-scoped callers can never aggregate another user's rows), tenant isolation via RLS. Web /reports page with side switch, date range (default trailing 6 months), groupBy/granularity/caliber controls, totals row + un-costed count + active caliber/base currency, graceful 403 notice, nav link + route (export deferred). Pushed (commits cabeb1e backend, a7e6180 web; 784b34d plan doc).

- Phase 1F-E: Commission calculation — migration 033 (commission_tables + commission_rate_rules mutable rate model; commission_settlements + commission_settlement_lines immutable append-only lock snapshot; RLS tenant isolation on all four, settlement pair granted SELECT,INSERT with UPDATE/DELETE revoked, reversible up/down verified). Backend commission module: GET /api/commission/summary + /orders compute commission on the 1F-D realized caliber (confirmed+completed) over frozen total_amount_base with per-salesperson rollup + per-order detail and dataScope pushed before aggregation; rate resolution rule → default_rate → 0-with-flag (rateSource: rule|default|none); POST /settlements snapshots rates + figures (immutable, append-only, idempotent re-lock, unlock as superseding append with required reason, editing a locked table → 409, all under SELECT … FOR UPDATE); money math in BigInt integer cents (commission-money.ts, zero float, serialized as numeric(18,2) decimal strings); RBAC commission_tables:view/:lock/:unlock; writes audited (created/updated/rules.replaced/locked/unlocked), reads not. Web commission pages added. Full quality gate green (integration 219, security 13/13); pushed (commits 16235b7 migration, 0493a3c backend, 1f07645 web, 6e43567 docs).

- Phase 1F-F: Commission payout / disbursement — migration 034 (commission_payouts batch header + commission_payout_lines, FORCE RLS, partial-unique no-double-pay index on settlement_id WHERE status<>'void', SELECT/INSERT/UPDATE grants with DELETE revoked, freeze-money-columns BEFORE UPDATE triggers on amount_base / total_payout_base), RBAC seed (commission_payouts:view/:disburse/:reverse in the finance group, separate from commission_tables:* for separation of duties), backend module (CommissionPayoutService + controller + DTOs under api/commission: amount-copy from a current-locked settlement, idempotent create, only-locked-payable 409, row-locked open→paid→void / pending→paid→void status machine with no auto-close, void frees the settlement, dataScope-aware reads, audit on every write), and web pages (CommissionPayoutsListPage + CommissionPayoutDetailPage with disburse/reverse actions, generate-or-view from settlement detail, base-currency display, graceful 403). Money is copied not computed (numeric(18,2), no FX). Full quality gate green (lint / format / typecheck / build / unit 7 / integration 235 / security 13); pushed (commits bad183d migration, 6baf4d1 seed, bb8631a backend, b30d5f3 web, 8368e19 integration tests). Browser QA passed (21/21): base-currency display (RMB total + per-line copied amounts), idempotent generate / no-double-pay (same live payout on regenerate, exactly one non-void payout per settlement), batch disburse → paid, freeze-after-paid read-only (no disburse controls + DB triggers reject amount_base / total_payout_base UPDATE), void frees the settlement to regenerate a new payout, and 403 fallback (view-only user blocked from disburse/void, no-finance user blocked from list + detail).

- Phase 1G: AI/OCR provider abstraction — backend + web complete. Plan in docs/phase-1g-ai-ocr-plan.md (backend) and docs/phase-1g-web-plan.md (web). Mock-only by design (CLAUDE.md §7: no real DeepSeek/OpenAI/OCR vendor wired). Migration 035 reuses the existing provider_invocations table (no new business table): adds nullable source_file_id (FK → files ON DELETE SET NULL, so deleting a file never cascades away the invocation record) and revokes UPDATE/DELETE on provider_invocations (append-only, like audit_logs); up/down/up reversibility verified. OCR + AI provider interfaces (OcrProvider / AiProvider) behind DI tokens (OCR_PROVIDER / AI_PROVIDER); deterministic MockOcrProvider / MockAiProvider (no network, no SDK, no API key, injectable delay for the timeout path + __force_error__ sentinel for the error path); vendor-neutral errors (Ocr/AiProviderException, Ocr/AiTimeoutException, FileNotInScopeException). Provider factory reads AI_OCR_PROVIDER, accepts only `mock`, and fails fast at startup on any other value (no silent fallback to a real vendor). AiService double-writes on success AND failure/timeout: one provider_invocations row (summaries only — fileId/docType in, fieldCount/confidence/textLength out; AI records inputLength, never the input text) plus one audit_logs event (provider.ocr|ai.invoked / .failed, resource_type=provider_invocation, resourceId links the two), provider error re-thrown after recording. AiController (api/ai) under TenantAuthGuard + PermissionGuard: POST /ocr (ocr:process), GET /ocr + /ocr/:id (ocr:view), POST /complete (ai:process), GET /complete + /complete/:id (ai:view); fileId scope-checked before the provider call, dataScope pushed into list/getOne (own sees only own; out-of-scope/cross-tenant → opaque 404); no update/delete routes (append-only). RBAC seed: module ai (id …008) + ocr:view/:process + ai:view/:process (Dev Admin auto-binds via FROM permissions p). Full quality gate green (unit incl. 31 ai unit tests, integration 256 incl. 21 ai integration tests covering mock success/error paths, 401/403 RBAC, dataScope + cross-tenant isolation, and the double-write with no-raw-text / no-input-leak assertions, security 13/13); pushed (commits b79db39 migration, f0627e9 OCR provider, 2ad4b07 AI provider, 794e37f factory/module, bd6238c service, eb1a238 controller, 94b6140 seed, 6feb6be integration tests).

  Web (Phase 1G continuation): two pages under apps/web/src/ai/ — OcrPage (`/ai/ocr`: file-id + docType form → POST /api/ai/ocr, deterministic field table + text snippet + confidence; below it a paginated invocation list + page-local getOne detail) and CompletePage (`/ai/complete`: task + input → POST /api/ai/complete, output panel + list + detail). Six api-client methods (ocrExtract/listOcr/getOcr/aiComplete/listAiCompletions/getAiCompletion) over the existing request<T>() wrapper; types in lib/types.ts mirror the backend summary shape (no tenant_id/request_json/response_json). Routes added to App.tsx; two nav links in AppLayout. Nav uses the app's established always-show + server-403 graceful-degradation convention (not permission-gated hiding — /api/auth/me carries no permission codes, and UI hiding is not a security boundary per §4); a no-perm user lands on the page and the list region renders a 没有权限 notice. Live OCR text / AI output are held only in React state (never persisted by the backend, and the frontend never writes them to localStorage/sessionStorage/URL or console). Pure-frontend change: no migration, no backend edit, no new dependency. Pushed (commits f196642 api-client+types, b996124 OcrPage, 0f5eebb CompletePage, 29347b2 routes+nav).

  Browser QA: this dev environment has no headless browser (Playwright/Puppeteer absent), so DOM rendering was not script-driven. Instead both dev servers were started for real (API on :3001 with all six /api/ai/* routes mapped; web on :3000 proxying /api → :3001) and the exact endpoints the pages call were exercised through the Vite proxy, covering the pages' full data path. Verified: file upload → in-scope fileId; OCR invoice success (MOCK-INV-0001 / amount 1000.00, [[MOCK OCR]] text, confidence 0.95); list + getOne return summary only (no text/fields); non-uuid → 400, unknown/out-of-scope fileId → 404, __force_error__ → 500; AI extract-order-fields success (deterministic JSON, tokensUsed null); all four no-perm routes → 403 (both list regions hit the forbidden branch). DB checks: provider_invocations holds three rows (ocr success / ocr error / ai success) with summaries only — request_json {fileId,docType} / {task,inputLength:14}, response_json {confidence,fieldCount,textLength} / {outputLength,tokensUsed}; audit_logs has the matching provider.ocr.invoked / provider.ocr.failed / provider.ai.invoked events (success AND failure both recorded); a raw-text leak scan for MOCK OCR / minimized text / MOCK CUSTOMER returned 0 rows; dev-tenant audit hash-chain verify-chain PASS (202 entries). Known limitation: the §6.2 step-9 in-browser DevTools privacy check (localStorage/console/URL inspection) needs a human browser and was not run here — but two automation-level proofs stand in for it: (1) the DB stores no raw OCR/AI content, and (2) a grep of OcrPage.tsx/CompletePage.tsx finds zero console.*, zero localStorage/sessionStorage, and zero URL/history writes, so the live results never escape React state.

- Phase 1H: Tenant RBAC role/user management — backend + web + browser QA complete. Plan in docs/phase-1h-rbac-role-editor-plan.md. Zero migration (reuses existing users / roles / user_roles / role_permissions tables + the permissions/modules dictionary, all already FORCE-RLS tenant-isolated); no new permission codes (lands the already-seeded users:view/create/update + roles:view/create/update under the system module). Backend users module (commit f0d2aa0): @Controller('api/users') under TenantAuthGuard + PermissionGuard — list (page/q/status, dataScope narrowed) / getOne (+ roles) / create (bcrypt initial password, optional roleIds) / update (name/phone/status) / PUT :id/roles (full-replace) / DELETE (soft-delete). Backend roles module (commit 473801e): @Controller('api/roles') — list (+ permission/user counts) / getOne (+ permission grants) / create / update / DELETE, PUT :id/permissions (full-replace grants), plus read-only @Controller('api/permissions') catalog grouped by module for the matrix. Server-side guards (CLAUDE.md §4, never UI-only): subset/no-privilege-escalation (caller can only grant codes it holds, at a data_scope ⊆ its own — via RbacService.listEffectivePermissions + scopeWithin), last-active-owner protection (409), self-lock protection (409), system-role read-only (is_system → 403), delete-blocked-while-referenced (409), duplicate email/name (409), is_tenant_owner never settable via DTO whitelist, cross-tenant → opaque 404 by RLS. Every write audited (user.created/updated/deactivated/roles_replaced, role.created/updated/deleted/permissions_replaced) — identifiers + diffs only, never passwords. Full quality gate green (integration 256, security 13/13). Web (commit 59ed4ba): apps/web/src/users/ (UsersListPage — list + search/status filter + enable/disable toggle via PATCH status; UserFormPage — create with initial password + role-assignment checkboxes, edit name/phone/status, email immutable) and apps/web/src/roles/ (RolesListPage — counts + system-role read-only 查看/disabled 删除; RolePermissionsPage — name/description + permission matrix grouped by module with per-grant data_scope dropdown, system roles render read-only), wired into App.tsx routes + AppLayout nav (用户 / 角色), api-client + lib/types extended; graceful-403 degradation on list surfaces; all server-side guards surface as mapped 403/409 notices. Pure-frontend, no new dep. Browser QA (Playwright + real Chromium, both dev servers live on :3001/:5173 through the Vite proxy, 7 screenshots): login → users list (3 rows, status/owner/actions) → roles list (system roles read-only) → create custom role via permission matrix (46 perms grouped by module, scope dropdowns appear on check) → role round-trips on reopen (2 checked) → create user assigning the new role → user appears in list on search → user's role round-trips on edit. All steps passed. Note: the seeded dev roles use synthetic non-v4 UUIDs that the pre-existing CreateUserDto @IsUUID('4') rejects (400) — a dev-fixture artifact only; API-created roles get real v4 UUIDs, so the QA created a fresh role first and assigned that.

- Phase 1I: Audit log viewer — backend + web + browser QA complete. Plan in docs/phase-1i-audit-viewer-plan.md. Read-only consumer of the existing audit_logs + audit_log_chains hash-chain infrastructure (migrations 015/016/021/022/023): zero migration, zero new permission code (audit_logs:view already seeded under the system module), write path (AuditService / hash algorithm / hash_version / chain structure / append-only triggers) untouched. Backend (commit bc59268): new AuditViewerModule (imports Rbac/Audit/Auth like ReportsModule, to avoid the AuditModule↔Rbac/Auth cycle) ships AuditController @Controller('api/audit-logs') under TenantAuthGuard + PermissionGuard, all gated by audit_logs:view — GET / (list: page/pageSize + from/to/actorId/actorType/action/resourceType/resourceId/requestId filters parameterized into WHERE, created_at DESC + id DESC, dataScope pushed in), GET /chain/verify (tenant chain integrity), GET /:id(\d+) (single event detail with before/after/metadata). Read-only AuditQueryService (separate from write-side AuditService): RLS tenant isolation via withTenantContext; own dataScope anchors to actor_id (audit rows carry no resource-owner field); opaque 404 on cross-tenant/missing; id surfaced as string (bigint precision-safe); actorName via tenant-safe LEFT JOIN users; mapper omits row_hash/prev_hash/hash_version. chain_key is derived server-side from the authenticated tenantId and never accepted from the client (audit_log_chains has no RLS — §2.2/§4.1.2). audit-chain.verify.ts mirrors packages/database/src/verify-chain.ts byte-for-byte (CommonJS API can't import the ESM db package, same reason AuditService duplicates the write-side hash) so the endpoint and `pnpm db:verify-chain` never disagree. Full quality gate green (integration 256, security 13/13). Viewing audit does not itself write audit (read endpoints unaudited, like reports/commission); the no-perm path still leaves the existing rbac:permission_denied trail. Web (commit 79a560d): apps/web/src/audit/AuditLogsPage.tsx (route /audit-logs) — one page with a chain-integrity status bar (green 审计链完整 ✓ 共 N 条 / red on failure / grey when unavailable), filters (default trailing 7-day window, draft→applied on submit), list (time / actor[actorName ?? actorId + type tag] / action / resource) + offset pagination, and a page-local detail panel rendering a field-level before/after diff over the union of keys (新增/移除/变更) with metadata/reason/ip/UA and a JSON fallback for non-object snapshots; full-page graceful-403 (没有权限查看审计日志). lib/types + api-client extended (listAuditLogs/getAuditLog/verifyAuditChain + auditQs); App.tsx route + AppLayout nav 审计 (always-show + server-403 per §4). Pure-frontend, no new dep. Web fix found in QA: the date picker emits a bare YYYY-MM-DD, which the backend's created_at <= to resolved to that day's 00:00 (so to=today hid the day's own events) — the page now expands to to an inclusive end-of-day timestamp before querying, keeping the backend's literal <= to contract. Browser QA (Playwright + real Chromium, both dev servers live on :3001/:5173 via the Vite proxy, 4 screenshots) all passed: nav→/audit-logs, green chain bar matching the CLI (verify-chain PASS), list + headers, actorType+action filter (correct subset), before/after field-level diff (the user.created snapshot shows no password field), no-perm user full-page 403 with no table, and an in-browser privacy check (no audit content in local/sessionStorage, clean URL, zero genuine app console errors — the 4 native 403 resource logs are the graceful-degradation path working). Note: a dev no-perm test user (qa-noperm-…@dev.local, no roles) was created in dev-tenant for the 403 step and left in place (harmless dev data, same as prior QA).

- Phase 1J: Data export (reports + audit CSV) — backend + tests + web + browser QA complete. Plan in docs/phase-1j-data-export-plan.md. Read-only export over the existing ReportsService (1F-D) and AuditQueryService (1I): same query, another serialization, plus one audited event. Zero migration, no new permission code (reuses reports:view / audit_logs:view), CSV-only (no Excel dependency; .xlsx deferred to a future phase per §4.1/§4.7). Backend (commit f520f6f): common/export-csv.ts is a zero-dependency CSV serializer — UTF-8 BOM + CRLF + RFC 4180 escaping + formula-injection neutralization on text cells (= + - @ TAB CR → leading apostrophe), numeric cells exempt so negative amounts stay numbers; common/export-response.ts sets attachment download headers (filename + RFC 5987 filename*, Cache-Control: no-store). GET /api/reports/sales-summary/export & /purchase-summary/export (reports:view) reuse ReportsService.salesSummary/purchaseSummary → self-describing CSV (preamble + rows + totals) via a new ReportsExportService. GET /api/audit-logs/export (audit_logs:view, declared before :id): AuditQueryService gains a shared buildWhere() (list refactored onto it, behavior unchanged — confirmed by the 256-test suite staying green) + a read-only listForExport (single LIMIT cap+1, cap=50000, truncated flag, no silent drop); a new AuditExportService exports ONLY the summary columns (no before/after/metadata/reason/ip/ua/requestId/hash). Every successful export writes one report.exported / audit_logs.exported audit event, fail-closed (audit BEFORE bytes — if the audit write throws the export throws and no file is delivered; stricter than Files' best-effort download because export is bulk egress); metadata is identifiers + filter summary + rowCount + truncated only, never row content. DTOs reject format!='csv' (400, the @IsIn(['csv'])) and unknown fields; the audit export DTO omits page/pageSize. Tests (commit 5ed9d04): fixtures gained reports:view (reports module, newly inserted) + audit_logs:view (system module) grants for admin/sales/tenant2 roles (test fixture only, no product seed/migration); export-csv.test.ts (serializer: BOM/CRLF/escaping/injection/numeric-exempt/null/ragged/timestamp), reports-export + audit-export service unit tests (serialize-and-audit-once, metadata shape no plaintext, truncation marker, fail-closed), and export.integration.test.ts (reports CSV headers/BOM/preamble/totals; CSV-vs-JSON value parity = no drift; wrong-side groupBy 400; format=xlsx 400; audit 7-column output with no snapshot/chain columns; filtered export == list with same filter; own-scope isolation; cross-tenant isolation; 401/403 RBAC; export-itself-audited with chain verify PASS; cap/truncation via direct service call with a tiny cap). Full quality gate green: unit 45, integration 268, security 13/13. Web (commit 164f4c5): pure-additive export buttons — no new page/route/nav, no new dependency. lib/download.ts (saveBlob: transient object URL revoked immediately, nothing persisted) + lib/api-client.ts low-level downloadBlob (shares auth header + ApiError mapping, returns blob + Content-Disposition filename) + exportSalesSummary/exportPurchaseSummary/exportAuditLogs (audit version drops page/pageSize). ReportsPage 导出 CSV button uses the current controls (same query the table reflects); AuditLogsPage 导出 CSV uses the applied filter + the same inclusiveTo 'today' boundary as the list; both add exporting/exportError state and map 403 → 没有权限导出… (defensive; same permission as view). Browser QA (Playwright + real Chromium, both dev servers live on :3001/:5173 via the Vite proxy, 3 screenshots) all passed: reports CSV download (filename/BOM/preamble/totals), audit CSV download (filename/BOM/7-column header/no snapshot-or-chain columns), in-browser privacy (no CSV content in local/sessionStorage, clean URL, zero genuine app console errors — the native 403 resource logs are the graceful-degradation path), and the no-perm user has no usable export (audit page fully 403-degraded with no export button; reports export button disabled).

- Phase 1K-A: Tenant lifecycle (platform-side suspend/activate/deactivate + global status gate) — backend complete (1K split into 1K-A lifecycle+gate and 1K-B support access per plan §1.4; 1K-B now complete — see the 1K-B entry below). The 1K-A platform web console deferred at the time landed with 1K-B (PlatformTenantsPage). Plan in docs/phase-1k-platform-access-tenant-lifecycle-plan.md. Migration 036 (commit 35de81b, an automated backup-cron snapshot that swept the working tree — not a hand-authored commit): tenants gains suspended_at + suspended_reason columns + chk_tenants_status CHECK (status IN active/suspended/deactivated); the status column + idx_tenants_status already existed; up/down reversible. tenants has NO RLS (global registry). Platform-side module under @Controller('api/platform/tenants') guarded by PlatformAuthGuard (platform-admin JWT only, separate identity per §4): GET / (list, page/pageSize/status filter, metadata only), GET /:id (metadata only, 404 if missing), POST /:id/suspend|/deactivate|/activate (@HttpCode 200). PlatformTenantsService.transition() runs under SELECT … FOR UPDATE in one tx: a TRANSITIONS state machine (suspend: active→suspended; deactivate: active|suspended→deactivated; activate: suspended|deactivated→active) → 409 on illegal transition, 404 on missing; UPDATE stamps suspended_at/reason on suspend|deactivate and NULLs them on activate; suspend/deactivate require a non-empty reason (TenantReasonDto, 400 otherwise). Every transition audits into the TENANT's own hash chain (actorType=platform_admin, action tenant.suspended/.activated/.deactivated, reason + {fromStatus,toStatus} metadata) via AuditService — whose session-actor=system insert is permitted by the existing audit_logs_system_insert policy, so NO new audit_logs RLS policy was needed (the full-1K plan §2.5 anticipated one; determined unnecessary for 1K-A). Service responses are metadata only — never tenant business data. Global gate: TenantStatusMiddleware (forRoutes('*')) verifies the bearer with the TENANT secret and no-ops on anything that is not a valid tenant token (platform tokens / unauth fall through to the route guards); for a valid tenant token it looks up the tenant status and throws 403 租户已停用 if not 'active' — blocking already-issued tokens after suspension (AuthService.login already refuses non-active tenants at login). The middleware is applied from TenantLifecycleModule, NOT AppModule (commit 9bd5456, the DI fix): applying it in AppModule.configure() made Nest resolve JwtService from AppModule's injector, which has no JwtModule, crashing startup with UnknownDependenciesException; TenantLifecycleModule owns the JwtModule.register({ TENANT secret }) so configure() there resolves correctly. Full quality gate green (lint / format / typecheck / build / unit 45 / integration 268 / security 13/13). Backend smoke QA (real API on :3001, throwaway tenant only — never the shared dev-tenant per plan §6.1, tenant + chain cleaned up after): all 5 routes map; list→200, getOne→200 (metadata only), suspend→200, re-suspend→409, no-reason→400, activate→200, deactivate→200; gate verified — active tenant token passes, suspended → 403 租户已停用, platform endpoints never gated; the three lifecycle audit events land in the tenant chain with chain verify PASS, and the append-only trigger correctly blocked the cleanup DELETE. Design note surfaced (not a 1K-A regression — matches how AuditService already behaves app-wide): AuditService silently no-ops if a tenant has no audit_log_chains row; every seeded/real tenant has one, but there is no tenant-provisioning service in code yet, so when tenant creation lands (1K-B or later) it must insert the chain row like seed 005 does. No web UI yet (platform-admin console deferred); no browser QA (platform endpoints are admin-API only). Not committed by me beyond the DI fix — the bulk landed via the backup-cron snapshot 35de81b; both are on origin/main.

- Phase 1K-B: Platform support access (customer-authorized, time-limited, scoped, audited) — backend + integration tests + web + browser QA complete. Plan in docs/phase-1k-platform-access-tenant-lifecycle-plan.md. The customer-trust cornerstone (CLAUDE.md §3): a platform admin can read a tenant's data ONLY via an authorization the tenant itself grants — named admin, reason, read_only scope, expiry — never by default; every grant lifecycle transition AND every authorized read is audited into the TENANT's own hash chain so the customer sees "who accessed me, when, under which grant, why" in the 1I viewer. Migration 037: support_access_grants (FORCE RLS tenant_isolation; partial unique index uq_sag_one_active on (platform_admin_id, tenant_id) WHERE status='active' blocks duplicate live grants; BEFORE UPDATE freeze trigger sag_freeze_immutable() rejects changes to the authorization TERMS — tenant_id/platform_admin_id/scope/reason/expires_at/granted_by_user_id via IS DISTINCT FROM — while status/revoke stamps stay mutable; two SECURITY DEFINER helpers app_check_support_access (validity = status='active' AND now()<expires_at) and app_list_support_grants_for_admin, search_path-pinned); up/down/up reversible. RBAC seed: support_access:grant/:revoke/:view under the system module (tenant owner/admin only). Tenant-side @Controller('api/support-access') under TenantAuthGuard + PermissionGuard: POST (grant — platformAdminEmail→id resolution returns opaque 404 for unknown/inactive admin so existence never leaks, dataScope=all since grants bind no resource owner, duplicate-active→409, past expiresAt/non-read_only→400, audit support_access.granted), GET list/:id (support_access:view, RLS tenant-bound), POST /:id/revoke (reason required, re-revoke→409, audit support_access.revoked). Platform-side read-only support access (PlatformAuthGuard + SupportAccessGuard, separate identity per §4): GET /api/platform/support/grants ("which tenants named me" via app_list_support_grants_for_admin on the authenticated admin id only), GET /api/platform/support/tenants/:tenantId/{audit-logs,audit-logs/:id,audit-logs/chain/verify,users,roles} — each requires an active unexpired grant naming this admin (else 403 default-deny), opens a withTenantContext({tenantId, userId:adminId, actorType:'platform_admin'}) limited read over existing 1I/1H services, writes one support_access.accessed into the tenant chain BEFORE the read; read_only is structurally guaranteed (no write/mutate route exists at all, not just a flag). Cross-tenant: a grant for T1 can't read T2 (403); grants listing names only the calling admin. Integration tests (commit 3507b20): support-access.integration.test.ts (21 cases) covering tenant grant/list/get/revoke + 404/400/409/403/401, the freeze trigger rejecting term changes (DB layer via owner conn), kirindesk_app having no DELETE, platform default-deny / active-grant-200-with-one-.accessed / expired-and-revoked→403 / cross-tenant→403, tenant-sees-.accessed in 1I, tenant chain verify PASS, .accessed metadata = exactly [resourceType,route,scope] (no business plaintext), and the SECURITY DEFINER helpers returning only the named admin's active grants; fixtures.ts grants SUPPORT_ACCESS_PERMS to admin roles only (sales/no-role stay without for 403 cases). Surfaced + fixed a real cross-identity audit isolation defect (commit 3507b20): the platform support read session sets BOTH tenantId AND actorType='platform_admin', and the audit_logs_tenant_read RLS policy ALSO exposes platform-global (tenant_id IS NULL) rows to a platform_admin actor — so AuditQueryService list/getOne/verifyTenantChain interleaved foreign-chain rows; fixed by pinning al.tenant_id = actor.tenantId explicitly in all three (no-op for tenant_user, which RLS already pins; 1I + 1J suites stayed green). Full quality gate green (unit 45, integration 289, security 13/13). Web (commits cf87887 support-access pages + 5df0c24 platform tenant lifecycle page): TWO isolated identity faces over the 1K-B + 1K-A backends, no new dependency. Tenant side — support-access/SupportAccessPage.tsx (/support-access, nav 支持访问): grant list with UI-DERIVED effective status (active past expires_at → 已过期), create (email/reason/read_only/datetime-local default 24h), revoke (reason required, active rows only), 404/409/400 mapped inline, whole-page graceful-403, header links to /audit-logs?action=support_access.accessed (the §5.2 1I trust loop, no new page). Platform side — a SECOND fully-isolated auth subtree: lib/platform-client.ts (platformRequest<T>() on its OWN token key kd_platform_token + its OWN 401→platform-logout hook, shares nothing with the tenant kd_access_token client, §4 no-impersonation), platform/ PlatformAuthContext / PlatformProtectedRoute / PlatformLayout (dark header, nav 租户 + 我的授权) / PlatformLoginPage (email+password, no tenantSlug) / PlatformGrantsPage (/platform/support-grants, "which tenants named me", live grants link in) / PlatformTenantViewPage (/platform/support/tenants/:tenantId, read-only audit/users/roles tabs + prominent "read-only, recorded, tenant-visible" banner + ZERO write controls + 403 full-page) / PlatformTenantsPage (/platform/tenants, the 1K-A lifecycle half: metadata-only tenant list + status filter + pagination + three reason-prompted actions 暂停/恢复/停用 mapped to the real 1K-A POST /:id/{suspend,deactivate,activate} endpoints — NOT the plan's earlier PATCH /status sketch — 409/404/400 surface inline, never renders business data); App.tsx mounts /platform/* as a separate Routes tree under its own PlatformAuthProvider, isolated from the tenant AppLayout. Browser QA (Playwright + real Chromium, both dev servers live on :3001/:5173 via the Vite proxy, 11 screenshots) all 22/22 passed: tenant grant→生效中→revoke→已撤销, audit shows granted/revoked, no-perm user full-page 403, kd_access_token present + kd_platform_token absent, grant reason/platform email not in storage, no genuine console errors; platform login, tenants list with no business-data columns, throwaway tenant (qa-throwaway, NEVER the shared dev-tenant per plan §6.1) suspend→已暂停→reactivate→运行中, my-grants 生效中, read-only view "已被记录" banner + zero write controls, kd_platform_token present + kd_access_token absent; the trust loop verified — after the platform read the tenant sees support_access.accessed in 1I, audit actor_type correct (accessed/suspended/activated=platform_admin, granted/revoked=tenant_user), both tenant chains verify-chain PASS. Note: QA created a throwaway tenant qa-throwaway (+ its audit_log_chains row) and set a known dev password on the existing no-perm user — harmless dev data left in the dev DB (same convention as prior QA); throwaway restored to active, no dangling suspended state.

- Phase 1L: Tenant onboarding (platform-side atomic provisioning + audit chain genesis) — backend + integration tests + web + browser QA complete. Plan in docs/phase-1l-tenant-onboarding-plan.md. Repays the 1K-A tech debt: every new tenant now gets its audit_log_chains genesis row atomically at creation, so AuditService never silently no-ops for a new tenant. Zero migration (reuses tenants / users / audit_log_chains — all three tables already exist). Backend (commit 1ef7dae): TenantOnboardingService.provision() — bcrypt(12) outside the transaction, then BEGIN: ① INSERT tenants (slug UNIQUE → 23505 → 409), ② SET LOCAL app.current_tenant_id + actor_type='system' for users FORCE RLS, ③ INSERT users (is_tenant_owner=true, status=active, email UNIQUE within tenant → 409), ④ UPDATE tenants SET owner_user_id (bare uuid, no FK — safe circular-ref workaround per §2.5), ⑤ INSERT audit_log_chains genesis (chain_key='tenant:<id>', last_hash=repeat('0',64)) — COMMIT, then best-effort post-commit AuditService.log(tenant.created, actor_type=platform_admin, metadata={tenantSlug,ownerEmail,ownerUserId}, no password). Response DTO (TenantOnboardingResult) uses explicit column whitelist — password_hash / ownerPassword never appear in response or audit. Wired into existing PlatformTenantsController + PlatformAuthGuard (platform-jwt only; tenant-jwt → 401). Integration tests: tenant-onboarding.integration.test.ts covering 201 atomic success (three rows in DB + genesis chain_key), owner login → 200, GET /api/customers → 200, verify-chain PASS (total ≥ 1 with tenant.created), audit metadata no-password assertion (JSON.stringify not containing password_hash / plaintext), slug conflict → 409 + rollback (no new rows in any of the three tables), slug format invalid / ownerPassword < 8 / missing field / extra field → 400, tenant-token / no-token → 401; quality gate green (unit 45, integration ~301, security 13/13). Web (commit cddb3cf): three additive changes, zero new routes/files. lib/types.ts: CreateTenantInput + TenantOnboardingResult interfaces (ownerPassword comment: only lives in memory, never written to storage/URL). lib/platform-client.ts: provisionTenant(input) → POST /api/platform/tenants (method=POST, body=input). PlatformTenantsPage.tsx: 「开通租户」toggle button in the toolbar + ProvisionPanel inline component (name/slug/ownerEmail/ownerPassword[type=password,autocomplete=new-password]/ownerName/contactEmail/contactPhone fields; 409-slug → 该标识已被占用; 409-email → Owner邮箱已存在; 400 → server message; ownerPassword cleared from React state immediately on success or error; success → panel collapses + load() + 已开通:<name>(<slug>) green notice; submitting disables all fields + button). Browser QA (Playwright + real Chromium, both dev servers live on :3001/:5173 via Vite proxy, 4 screenshots) all 7/7 checks passed: login → nav to /platform/tenants → 开通租户 button visible; provision panel opens; form filled (screenshot 1); submit → 已开通 success + new slug in list (screenshot 2); same slug resubmit → 该标识已被占用 inline, page intact (screenshot 3); unauthenticated page2 goto /platform/tenants → redirect to /platform/login; privacy spot-check: TestPass not in localStorage/sessionStorage, URL stays /platform/tenants, zero genuine console errors (screenshot 4). Known limitations (same as prior QA convention): throwaway tenant from QA left in dev DB (active, harmless dev data).

- Phase 1M: Subscription quota management — backend + integration tests + frontend complete. Plan in docs/phase-1m-subscription-quota-plan.md. Migration 038 (plan_id + plan_assigned_at + plan_expires_at on tenants; tenant_quota_usage snapshot table with FORCE RLS, DELETE revoked). SubscriptionModule (@Global): SubscriptionService (GET /api/subscription returning plan + usage + modules; GET /api/mobile/v1/subscription mobile alias; platform GET /api/platform/plans, GET/PUT /api/platform/tenants/:id/subscription), QuotaService (checkQuota with BEGIN/SET LOCAL for FORCE RLS, increment/decrement/addStorage/subtractStorage/incrementAi with month-reset logic, insertInitialRow called atomically in TenantOnboardingService.provision()), QuotaGuard (@CheckQuota decorator, 429 QUOTA_EXCEEDED with quota/limit/current body, falls through on missing usage row), ModuleGuard (@RequireModule decorator, 403 MODULE_NOT_ENABLED, BEGIN/SET LOCAL for FORCE RLS on tenant_modules). QuotaGuard applied class-level on UsersController + FilesController + AiController; ModuleGuard + @RequireModule('ai') applied class-level on AiController. Service hooks: UserService.create/deactivate → quota.increment/decrement (best-effort, warn-on-fail); FilesService.upload/remove → quota.addStorage/subtractStorage; AiService.ocrExtract/aiComplete → quota.incrementAi on success only. AssignPlanDto uses @IsString/@IsNotEmpty (synthetic plan UUIDs fail class-validator @IsUUID). Fixtures updated: plans seed (003_plans.sql not run by pnpm migrate), tenant_modules seed (SELECT by module code, not hardcoded UUIDs), tenant_quota_usage seed, USER_MGMT_PERMS added to SEED_PERMS. Integration tests (subscription.integration.test.ts, 10 cases): quota_usage on provision, GET /api/subscription, 429 on user/storage/ai limits, month-rollover reset, platform plan assignment, ModuleGuard 403, mobile alias, RBAC. Full quality gate green (lint / format / typecheck / build / unit 45 / integration 314 / security 13/13). Web: SubscriptionPage.tsx (/subscription, nav「套餐」) — plan name/expiry, three quota progress bars (users/storage GB/AI calls), module tags; PlatformTenantsPage gains「套餐」column with PlanPanel modal (shows current usage + plan dropdown with PUT /api/platform/tenants/:id/subscription). Committed (commit b80984e).

- Phase 1N: Notification email module — backend + integration tests + frontend complete. Plan in docs/phase-1n-notification-email-plan.md. Mock-only by design (CLAUDE.md §7: no real SMTP/SES/SendGrid vendor wired; NOTIFICATION_PROVIDER reads env, accepts only `mock`, fails fast at startup on any other value). Migration 039: tenant_notification_settings (one row per tenant — order_events / user_welcome / support_access booleans default true + updated_at; FORCE RLS tns_tenant_isolation policy; SELECT/INSERT/UPDATE granted, DELETE revoked; up/down reversible). @Global NotificationModule (imports Audit/Rbac/Auth) so any service can inject NotificationService without an import cycle. EmailProvider interface + EMAIL_PROVIDER DI token behind a deterministic MockEmailProvider (static calls[] array for test assertions, throws on `__force_error__` subject for the failure path; no network, no SDK, no API key). NotificationService: send(tenantId, actorId, event, to, subject, text) checks the per-tenant per-event toggle (fail-open: defaults to send if settings unreadable), calls the provider, and double-records — audit notification.sent on success / notification.failed on provider error (into the TENANT's own hash chain, metadata = {event,to} or {event,to,error}, never message body beyond subject/to), re-throws after recording so fire-and-forget callers can swallow; getSettings/updateSettings via withTenantContext (RLS tenant-bound, COALESCE-based partial update, INSERT … ON CONFLICT upsert); insertInitialRow(client, tenantId) called atomically inside TenantOnboardingService.provision() (step ⑧) so every new tenant gets its settings genesis row in the same tx as the tenant/owner/quota/chain rows. NotificationController @Controller('api/notifications') under TenantAuthGuard + PermissionGuard: GET /settings (tenant_settings:view), PUT /settings (tenant_settings:update) — no new permission code (reuses the existing tenant_settings:* seed). Service hooks, all fire-and-forget (void send().catch(warn), business op never blocked or failed by a notification error): UsersService.create → user_welcome email; SalesOrdersService / PurchaseOrdersService approve|reject transition → order_events email to the original submitter (looked up from the order_approvals submit row); SupportAccessService.create → support_access grant notification. Integration tests (notification.integration.test.ts, 10 cases): provisioning creates the settings row with defaults, GET returns defaults, PUT toggles, user.created dispatches a welcome email, user_welcome=false suppresses it, provider-failure resilience (user create still 201), 401 without token, 403 without tenant_settings:update, audit chain has notification.sent after a welcome email, cross-tenant isolation; fixtures.ts seeds the settings row for both test tenants. Full quality gate green (lint / format / typecheck / build / unit 45 / integration 324 / security 13/13). Web: NotificationSettingsPage.tsx (/notifications, nav「通知」) toggle UI over GET/PUT /api/notifications/settings, api-client + lib/types extended, App.tsx route + AppLayout nav (always-show + server-403 graceful degradation per §4). Pure-additive frontend, no new dependency. Committed (commit ed1568b). Browser QA still to be done.

- Phase 2A: Billing & payment — backend + integration tests complete (web deferred). Plan in docs/phase-2a-billing-payment-plan.md. Mock-only by design (CLAUDE.md §7: no real WeChat Pay/Alipay/Stripe gateway wired; PAYMENT_PROVIDER reads env, accepts only `mock`, fails fast at startup on any other value). Migration 040: billing_invoices (one charge per tenant per period — plan_id FK, billing_period monthly|yearly CHECK, amount_cents bigint, currency, status pending|paid|void CHECK, issued_at/due_at/paid_at/void_reason; paid requires paid_at, void requires void_reason) + billing_payments (immutable provider charge record — invoice_id FK, provider, provider_ref, amount_cents, currency, status succeeded|failed CHECK). Both FORCE RLS tenant_isolation. Money is integer cents (bigint), frozen on invoices by a BEFORE UPDATE trigger (freeze_billing_invoice_amount) so amount_cents is write-once while UPDATE stays granted for the pending->paid|void status machine; invoices DELETE revoked; payments are write-once (SELECT/INSERT only, UPDATE+DELETE revoked). Partial unique index uq_billing_payments_succeeded_invoice (one succeeded payment per invoice) = the DB-level no-double-pay guard. Up/down reversible (verified migrate/rollback/migrate). BillingModule (imports Rbac/Audit/Auth): PaymentProvider interface + MockPaymentProvider behind PAYMENT_PROVIDER DI token via a factory that calls resolvePaymentProviderName() at startup (only `mock`, throws otherwise — switching to a real gateway is a deliberate approved code change, never a config fallback); MockPaymentProvider is deterministic (static calls[] for assertions, two failure hooks: currency==='__force_error__' sentinel + static failNext one-shot; no network/SDK/key). payment-money.ts BigInt decimalToCents/centsToDecimal (mirrors commission-money). BillingService: issueForTenant (platform-side, runs under the TENANT context as actorType=platform_admin so the audit lands in the tenant's own chain; amount + currency derived server-side from the plan price by period — never client-set; idempotent — reuses an existing pending invoice for the same plan+period rather than double-billing; audits billing.invoice_issued); list/getOne (tenant-side via withTenantContext, RLS-isolated, opaque 404 cross-tenant); pay (SELECT … FOR UPDATE, 409 if not pending; on provider success writes an immutable succeeded payment row + flips invoice to paid + paid_at in one tx, partial unique index is the backstop; on provider failure commits a `failed` payment row for the audit trail, LEAVES the invoice pending/unpolluted so it stays payable on retry, audits billing.payment_failed, throws 502). Tenant @Controller('api/billing') under TenantAuthGuard + PermissionGuard: GET /invoices + GET /invoices/:id (billing:view), POST /invoices/:id/pay (billing:pay, @HttpCode 200); platform @Controller POST /api/platform/tenants/:id/invoices (PlatformAuthGuard, 201 fresh / 200 idempotent). RBAC seed: billing:view / billing:pay under the existing finance module (db/seeds/002_permissions.sql; Dev Admin auto-binds via FROM permissions; no new module). Integration tests (billing.integration.test.ts, 9 cases): amount equals the tenant plan price + currency derived, idempotent issue (201 then 200 same id, total stays 1), pay success (invoice→paid + exactly one succeeded payment with provider=mock + MOCK-PAY- ref), double-pay→409 with no second payment row, provider-failure→502 + invoice stays pending + paidAt null + only a failed row + retry succeeds, cross-tenant isolation (tenant2 get/pay→404, list total 0), 401 without token, 403 without billing perms (no-role user), platform issue with a tenant token→401; fixtures.ts seeds BILLING_PERMS into the finance module for admin/sales roles (no-role user lacks them for the 403 case). Full quality gate green (lint / format / typecheck / build / unit 45 / integration 333 / security 13/13). Committed (commit a8fc433). Web (BillingPage + pay button) and browser QA deferred to the next 2A sub-step.

The next planned step is:

Phase 2A web (BillingPage: invoice list + status + pay button; api-client/types/route/nav) and browser QA, plus the still-pending browser QA for Phase 1M / 1N. Then the next billing/governance sub-phase or hardening of deferred items.

The Phase 0 foundation baseline is in place. Phase 1E (Files), Phase 1F-A (order line items), Phase 1F-B (finance/exchange-rate snapshot), Phase 1F-C (order approval workflow), Phase 1F-D (reports aggregation), Phase 1F-E (commission calculation), Phase 1F-F (commission payout / disbursement), Phase 1G (AI/OCR provider abstraction — backend + web), Phase 1H (tenant RBAC role/user management — backend + web + browser QA), Phase 1I (audit log viewer — backend + web + browser QA), Phase 1J (data export — reports + audit CSV, backend + tests + web + browser QA), Phase 1K-A (tenant lifecycle + global status gate — backend), Phase 1K-B (platform support access — backend + integration tests + web + browser QA), Phase 1L (tenant onboarding — atomic provision + audit chain genesis, backend + integration tests + web + browser QA), Phase 1M (subscription quota management — backend + integration tests + frontend), and Phase 1N (notification email module — backend + integration tests + frontend), and Phase 2A (billing & payment — backend + integration tests, web deferred) are complete. Do not start the next step implementation until its scope is planned and approved.
