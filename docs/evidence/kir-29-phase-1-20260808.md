# KIR-29 Phase 1 验收证据

日期：2026-08-08
分支：`feat/kir-29-document-workbench`

## 完成项

- 产品库、自定义字段值、停用/排序/单据范围配置与产品缩略图。
- 客户可为空的快速报价，可选关联既有客户和销售订单。
- QT、PI、SC、CI、PL 一次录入、草稿版本联动和数据库强制锁定快照。
- 最终报价与成本利润模式；成本权限独立，客户投影/PDF 无内部字段。
- 精确金额、双币种、汇率、折扣、三类贸易术语、费用分摊、重量体积与两类装箱。
- 六语固定模板、RTL、主题、显隐、条款、银行信息、LOGO、签章和缩略图。
- Chromium 真实 PDF、Files/对象存储归档、导出版本与审计链。
- 哈希令牌、固定旧版本、无自动过期、手动作废及打开/下载/确认事件。
- RLS、组合租户外键、RBAC、customers、sales-orders、files 和 audit 集成。

## 自动化结果

| 门禁 | 结果 |
| --- | --- |
| API 单元测试 | 19 files / 93 tests passed |
| KIR-29 专项集成测试 | 1 file / 8 tests passed |
| API lint | passed |
| Web lint | passed |
| API typecheck | passed |
| Web typecheck | passed |
| API build | passed |
| Web build | passed；仅有 500 kB chunk 警告 |
| 053 回滚/前滚演练 | passed；052/053 双迁移 round-trip passed |
| 051 历史升级回归 | passed；已发布校验和与自定义角色授权保持 |
| 真实 PDF 探针 | passed；`%PDF-`，183,990 bytes，含中文文本 |
| API Alpine runtime image | passed；Chromium、Noto、Noto CJK 安装并完成镜像构建 |

## 安全断言

- 跨租户使用其他租户 JWT 读取单证 ID 返回 404。
- 无成本权限但有单证读取权限的用户响应中不存在成本价、成本合计或内部合计。
- PDF 渲染测试替身收到的对象只有公开投影，不存在内部成本字段。
- 原始追踪令牌不落库；数据库值等于原始令牌的 SHA-256，且不等于原始令牌。
- 匿名令牌解析函数固定 `search_path`、撤销数据库 `PUBLIC` 执行权，并在解析层排除已作废链接。
- 更新草稿到 v2 后，v1 链接仍返回 v1 售价与快照。
- 打开、下载、确认各形成独立事件；作废后匿名读取返回统一 404。
- 重复确认返回首次确认时间且不重复写入确认事件。
- 锁定后 API 更新返回 409，数据库触发器同时阻止绕过服务修改。

## 复现命令

```bash
pnpm --filter @kirindesk/api test:unit
pnpm --filter @kirindesk/api exec vitest run --config vitest.integration.config.ts test/document-workbench.integration.test.ts
pnpm --filter @kirindesk/api lint
pnpm --filter @kirindesk/web lint
pnpm --filter @kirindesk/api typecheck
pnpm --filter @kirindesk/web typecheck
pnpm --filter @kirindesk/api build
pnpm --filter @kirindesk/web build
TEST_DATABASE_URL=postgresql://kirindesk:kirindesk_dev_password@localhost:5432/kirindesk_test pnpm verify:migrations
docker build --target runtime -f apps/api/Dockerfile -t kirindesk-api:kir-29 .
```

## 未完成与非目标

- 未执行生产发布、生产数据迁移或 main 合并。
- 未接入实时汇率、报关单、报关委托、离线编辑或桌面授权；这些均不属于 Phase 1。
- 未执行 main 合并；阶段分支与 PR 必须由 Kai 独立审核后再决定是否进入 main。

## 回滚摘要

优先回滚应用并保留 053。若必须数据库回滚，需先备份一期表与关联 Files 清单；053 的 DOWN 会删除一期业务数据，且对象存储清理需要单独受控执行。
